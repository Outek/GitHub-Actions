name: Build

on: push

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Build
        run: |
          pwd
          ./build.ps1
        shell: pwsh
      - name: Test
        run: ./Pester.Test.ps1
        shell: pwsh