name: Build

on: push

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Build
        run: |
          pwd
          ../build.ps1
        shell: pwsh
      - name: Test
        run: ./Pester.Test.ps1
        shell: pwsh